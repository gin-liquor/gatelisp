(module async-counter
  (ports
    (clk :in bit)
    (rst :in bit)
    (q   :out (unsigned 8)))

  (reg count (unsigned 8) 0)

  (clocked clk
    (reset :async rst
      (next count 0))

    (next count (+ count 1)))

  (assign q count))
