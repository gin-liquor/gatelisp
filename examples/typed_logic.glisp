(module typed-logic
  (ports
    (a      :in bit)
    (b      :in bit)
    (select :in bit)
    (y      :out bit))

  (wire combined bit)

  (assign combined (and a b))
  (assign y (if select combined (not combined))))
